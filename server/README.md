# Tetris Server

Simple Express server written in TypeScript that stores high scores in MongoDB.

## Development

Install dependencies and start in development mode:

```bash
npm install
npm run dev
```

The server listens on `http://localhost:3000` by default.
