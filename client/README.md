# Tetris Client

A simple Tetris implementation using React Native with Expo.

## Development

Install dependencies and run the app:

```bash
npm install
npm start
```

The Expo developer tools will open, allowing you to run the app on a simulator or device.
