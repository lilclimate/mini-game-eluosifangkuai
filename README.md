# mini-game-eluosifangkuai

This repository contains a simple Tetris game.

- `client/` — React Native (Expo) app implementing the game logic.
- `server/` — Node.js/TypeScript server using MongoDB to store high scores.

See the README in each folder for setup instructions.
